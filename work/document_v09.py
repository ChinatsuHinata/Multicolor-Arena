from pathlib import Path
import json,re
root=Path(__file__).resolve().parent.parent
manifest=json.loads((root/'work/v09-import-manifest.json').read_text('utf-8'))
rows=[]
for item in manifest:
 d=json.loads((root/f"cards/{item['id']}.json").read_text('utf-8'))
 cost=' '.join(f'{k}{v}' for k,v in d['费用'].items()) or '0'
 handlers=', '.join(a['参数']['效果'] if a['实现']=='extension' else a['实现'] for a in d['能力绑定'])
 rows.append(f"| {item['id']} | {d['名称']} | {d['类别']} | {cost} | {'、'.join(d.get('关键词',[]))} | {handlers or '关键词'} |")
table='\n'.join(rows)
(root/'docs/新增卡片导入清单-v0.9.md').write_text('''# v0.9 新增卡片导入清单

2026-09-18。实际目录为 `recourse/新增卡片`，共 45 个 PNG 与 1 个 JPG，共 46 张。与原有 15 张合计 61 张可构筑卡，另有局内生成的飞碟、半灵衍生物。原始图像和表格不改写。

每张卡对应 `cards/ID.json`。`来源`保留原牌表工作表与行号；莉莉卡原表数值格缺失，按 image29.png 明确补为红2黄1、攻击3、血量3、灵力2。扩展效果在 `scripts/rules/expanded_abilities.gd` 执行；读卡文不自动执行规则。

| ID | 名称 | 类别 | 费用 | 关键词 | 效果绑定 |
| --- | --- | --- | --- | --- | --- |
'''+table+'''

卡图读取时按既有方向转成竖版，统一为 1200×1676，再生成 mipmap。低分辨率图使用 Lanczos 插值放大；这统一显示规格，不恢复原图缺失的细节。原图的 sample 字样保留。

验证入口：`tests/test_v09_rules.gd`、`tests/test_v09_ui.gd`、`tests/test_v09_matches.gd`。整局报告见 `work/v09-matches.json`。规则断言涵盖进出场、死亡、墓地启动、模式、额外牺牲/弃牌费用、计时替代、结晶、奇迹、先制、终言和衍生物；不代表未登记全牌表或任意未来文本已能执行。
''','utf-8')
p=root/'使用说明.md';s=p.read_text('utf-8').replace('v0.8.2','v0.9')
s=s.replace('**查看墓地**：点墓地，在桌面铺开；再次点击翻页，右键展开的牌可查看。右键空白处收起。滚轮缩放桌面。','**查看墓地与除外区**：点对应牌堆打开卡片列表，可右键查看完整牌文；己方墓地中当前可发动的卡会显示「发动」。滚轮缩放桌面。')
s=s.replace('支持完整称号搜索、多色筛选、新建、重命名、保存、复制粘贴。','支持完整称号搜索、多色筛选、新建、重命名、保存、复制粘贴。顶部「排序卡组」分别整理主卡组和副卡组，按类别、总费用、完整卡名排序，相同牌相邻；张数和自机不变，点击保存后写入。')
s=s.replace('正常对战检查主卡 50 张、主卡同名最多 4 张、主副卡不含所选自机同名牌；测试开关跳过这三项','正常对战检查主卡 50 张、主卡同名最多 4 张（终言同名最多 1 张）、主副卡不含所选自机同名牌；测试开关跳过这些构筑检查')
s=s.replace('15 张卡的中文资料','61 张卡的中文资料').replace('`scripts/rules/demo_abilities.gd`：当前卡池能力实现。','`scripts/rules/demo_abilities.gd`、`expanded_abilities.gd`：原有和新增卡池能力实现。')
s=s.replace('当前可用范围是这 15 张卡的本地人机测试。全牌表、梦违运行时加入、通用替代效应、联网和独立导出尚未完成。','当前可用范围是 61 张卡的本地人机对局与本地调试；本批 46 张新卡已登记规则效果。全牌表、梦违运行时加入、通用复制/替代框架、联网和独立导出仍属后续计划。')
s+='''

## 关于与调试

主菜单进入「关于」，显示居中的「这是测试文字」。在页面直接用英文输入 `multicolor`，无需输入框或回车，即开启调试。页面出现「退出调试模式」可关闭；模式只存在本次运行，重启默认关闭。进入下一场对战时应用当前模式。

调试对局公开双方手牌。顶部「操作：你／人机」指示当前由你操作的一方；调度、凭依、出牌、付款、攻击、阻挡和响应按当前执行权或选择所属方切换，人机不自动代打。人机手牌也从上方手牌区拖出使用。三档响应仍按原定义工作。

「你的牌库」「人机牌库」可随时打开整副剩余牌库，保留正在进行的付款和选择。右键查看卡图和文字，角落可观察战场，再返回选择。

「调试」中选卡与目的地区域后点击「移动」；不创建重复实例，拥有者保持。支持手牌、牌库、战场、颜色盘、墓地、除外区和本局自机区。自机位仅容纳本局所选自机；普通符卡不能留在战场，单位受战场格和同称号限制，衍生物离场后消失。移入堆叠走正常使用、目标和付款流程；待处理选择、尚未提交的使用与已结束对局不会被直接改写。区域移动会产生对应触发。这些操作只影响当前对局，不改保存的卡组。

## 新增卡效果的操作

模式、额外费用及非战场目标通过「选择目标」列表声明，确认付款前可以取消。异星弹幕先选第一项再选第二项，可重复。触发能力出现「选择」；可选触发可以「不使用」。墓地卡可在墓地列表发动；满足条件时，Hell's Tokamak 可从「牌库可用牌」使用。结晶、贫乏、奇迹、计时与先制已接入本批卡的规则。

新卡清单与绑定见 `docs/新增卡片导入清单-v0.9.md`。卡图统一显示尺寸，原文件保持不变。
'''
p.write_text(s,'utf-8')
p=root/'docs/卡牌数据库与编写说明.md';s=p.read_text('utf-8').replace('版本：v0.8.2','版本：v0.9',1).replace('→ demo_abilities.gd 执行登记的能力','→ demo_abilities.gd / expanded_abilities.gd 执行登记的能力',1)
s=s.replace('## 当前卡池','## 原有 15 张卡池\n\n当前合计 61 张；新增 46 张完整清单见 [新增卡片导入清单](新增卡片导入清单-v0.9.md)。本节保留原卡资料。',1)
s=s.replace('当前的进场、触发、持续能力、临时强化、战斗关键词和支付入口覆盖已登记的15张卡，新增时符、复制、梦违或开局增牌仍须扩展引擎。','当前登记的61张卡有明确能力实现，包括本批计时符卡；通用复制、梦违或开局增牌仍须扩展引擎。',1)
s+='''

## v0.9：扩展能力与调试

数据库增加 `关键词`、`符卡类型`、`计时`。费用可以为 0；零费用卡仍需满足角色约束和目标条件。扩展绑定格式：

```json
"能力绑定": [
  {"实现": "extension", "参数": {"效果": "enter_haste"}}
]
```

`card_database.EXTENSION_EFFECTS`校验已登记效果。添加新效果必须同时实现事件入口、合法选择、结算和测试；仅在 JSON 填英文名称不构成实现。完整映射见新增清单。常用入口如下：

| 入口 | 责任 |
| --- | --- |
| `spell_options / spell_resolve` | 使用时的模式、目标、额外牺牲声明及符卡结算 |
| `on_enter / on_leave / on_death` | 进场、离场、死亡事件，使用最后已知信息 |
| `trigger_options / resolve_trigger` | 触发选择与堆叠结算；目标包括墓地、颜色盘、复合目标 |
| `activation_options / activation_cost / resolve_activation` | 墓地启动、横置移除、牺牲强化、每回合限次返回 |
| `duel_engine.move_to / detach / enter_field` | 实例换区、区域版本、进场合法性、自机返回和衍生物消失 |
| `delayed / run_delayed` | 指定下一准备／结束阶段的返回或牺牲，校验原实例区域版本 |
| `add_timer / choose_timer` | 本批咲夜的计时增加／减少替代选择 |
| `deals_combat_damage` | 先制与后续伤判响应窗口 |

`plus_counters`为持久数值指示物，换区清除；`modifiers`为本回合修正，可正可负。`poverty`限制凭依。时符留在战场但不是永久物，出鞘只移除永久物。反魂蝶计时清算只进行一次，归零按卡文失败；投降仍可结束。

飞碟和半灵在规则引擎初始化时登记局内定义，不暴露在常规组卡器。衍生物使用独立空白示意图，不借用其他卡的规则。普通人机的隐藏信息保持；只有当前本地会话的关于页暗号开启后，界面跟随执行权控制双方。所有调试移动都有引擎校验，不能靠界面绕过构筑存档保护。

当前策略是启发式人机；能使用本批新卡与合法高速响应、墓地启动，并不保证选择最强线路。通用同时触发排序编辑器、完整替代效应组合框架、复制与联网确定性回放仍在后续计划。
'''
p.write_text(s,'utf-8')
p=root/'docs/游戏整体开发计划.md';s=p.read_text('utf-8').replace('版本：计划 0.8 / 可运行实现 v0.8.2','版本：计划 0.9 / 可运行实现 v0.9',1)
s=s.replace('当前阶段：15张卡的本地人机对局已接入，战斗交互与观察模式已完善；全牌表扩展与联网仍在后续范围。','当前阶段：61张卡的本地对局、关于页调试入口、双边手动控制、牌库检查与合法区域移动已接入；全牌表扩展与联网仍在后续范围。',1)
s=s.replace('第18—23节，以第23节为最新修订','第18—24节，以第24节为最新修订',1)
s+='''

## 24. v0.9：本地调试与 46 张新卡（2026-09-18）

本轮按实际 `recourse/新增卡片` 导入全部 46 张，合计 61 张常规卡。原表缺失的莉莉卡数值按卡图补全；图片只在显示缓存中统一到1200×1676，原素材不改写。详细来源和效果绑定见 [新增清单](新增卡片导入清单-v0.9.md)。

| 状态 | 进入和操作 | 退出与保留数据 |
| --- | --- | --- |
| 关于 | 主菜单进入；标题和测试文字居中 | 返回主菜单 |
| 会话调试开关 | 关于页键入 multicolor | 可手动关闭，重启关闭；下一局应用 |
| 双边操作 | 随执行权、必须选择所属方切换控制方 | 普通模式固定控制玩家0，AI负责玩家1 |
| 区域浏览 | 双方牌库、墓地、除外区卡片列表；右键预览 | 不清空原目标、付款或必须选择 |
| 调试移动 | 选择已有实例和目的地区域；引擎验证 | 保留拥有者，产生对应事件，只改本局 |
| 复合声明 | 模式、一个或多个目标、额外牺牲或弃牌 | 提交前私有；提交时验证并一次付费 |
| 扩展触发选择 | 墓地／颜色盘目标、结晶、可选死亡或进场效果 | 进入既有堆叠；取消只适用于可选触发 |
| 先制伤判 | 先制参与者先造成伤害，再进行响应 | 存活者进入后续伤判，保持原战斗信息 |
| 计时替代 | 本批咲夜可调整指示物数量 | 处理完返回原动作，结束清算每回合只减一次 |
| 延迟事件 | 记录对象区域版本和阶段 | 对象改变区域版本则旧返回无效 |

新增结晶、先制、奇迹、终言、贫乏、持久+1指示物、临时负修正、伤害防止、不会被消灭、时符胜负保护、衍生物、墓地／牌库使用和本批计时替代。GDScript 承担规则与事件，JSON 明确数据与绑定，未用自然语言猜测执行。卡组排序分别处理主副卡，相同牌相邻，保留所有副本，标记草稿已修改。

验证：144项新卡及规则/构筑/调试边界检查、37项真实界面输入与显示检查、17项新卡池整局检查，加197项既有回归，共395项零失败。8场新增卡池合法50张对局均正常结束，人机实际使用44种新增牌；其余特定场景由规则断言覆盖。原15卡池六场完整对局及战斗动画、三档响应、费用取消等回归通过。玩家存档 SHA256 前后相同。

下一阶段仍按用户选择推进：更多卡牌和梦违；通用触发排序／替代组合／复制能力；联网命令与回放；人机策略和规则组合边界完善。此次实现面向登记的卡池，不等于完整牌表及所有未来特殊文本已自动支持。
'''
p.write_text(s,'utf-8')
p=root/'开发者日志.md';s=p.read_text('utf-8')
s+='''

## 2026-09-18 · v0.9 关于页、双边调试、46张卡与卡组排序

- 主菜单新增「关于」，文字居中且含「这是测试文字」。页面直接键入 `multicolor` 开启本次运行的调试模式，可退出；不写入用户设置。
- 调试对局展示人机全部手牌，人机停止自动行动；界面随执行权或必须选择所属方操作双方。双方牌库随时可查，保留付款、目标与原选择。增加调试区域移动面板，保留实例与拥有者，验证自机位、战场容量、同称号、符卡驻场、衍生物和对局状态；堆叠仍走正常使用/付费流程。查看、移动与观察战场互相切换。
- 实际素材目录 `recourse/新增卡片` 共46张全部登记，与原15张合计61张。以原牌表匹配来源和完整牌文；莉莉卡缺失数值按卡图补为红2黄1、3/3、灵力2。新增 GDScript 扩展能力模块，数据库拒绝未知实现。
- 接入本批进场、死亡、离场、自机能力、墓地启动、除外区、结晶、奇迹、先制、终言、贫乏、计时与咲夜替代、模式选择、额外牺牲/弃牌费用、符卡反制、持续灵力、持久指示物、临时负值修正、防止伤害、不可消灭、延迟返回和衍生物。反魂蝶按计时处理胜负；时符不被当作永久物。自机返回选项保留，衍生物离场消失。
- 扩展人机高速响应与墓地启动策略。新模式和目标通过明确选择提交，确认前不改变公开资源。原三档响应、攻防、伤害分配、动画和边框约定保留。
- 卡图显示缓存统一1200×1676并生成mipmap，低分辨率使用Lanczos放大；原图和sample水印不改写。牌库查看的长卡名留足换行区域。
- 组卡器新增「排序卡组」，主、副卡独立按类别、费用、完整卡名整理，相同卡相邻、不减少副本、不改自机，不自动保存。
- 更新使用说明、整体计划0.9、编写说明，并新增 `docs/新增卡片导入清单-v0.9.md`。源文件/卡牌/存档备份位于 `work/v09-backup/`，导入溯源位于 `work/v09-import-manifest.json`。

验证395项零失败：新规则144，真实GUI及调试37，新卡池整局17，既有核心规则36、15卡池整局14、v0.8.1交互20、三档响应41、支付交互28、v0.8动画58。8场新卡池合法50张对局全部结束，人机实际使用44种新牌；测试结果 `work/v09-test_v09_rules.log`、`work/v09-test_v09_matches.log`、`work/v09-ui-render.log`、`work/v09-matches.json`。真实渲染检查 `work/v09-about.png`、`work/v09-debug-battle.png`、`work/v09-debug-move.png`、`work/v09-deck-inspection.png`、`work/v09-editor.png`。

玩家 `saves/decks.json` 前后 SHA256 一致：`E8863509CBA8049FF5BC488488DD50D4414081A9C4050F5943CA44EE8DDBEA56`。文件写入限授权test目录；无项目脚本解析或运行错误，Godot仍有原有系统根证书读取诊断。人机为启发式策略，完整牌表、通用复制/替代组合、梦违、联网及独立导出仍待后续开发。
'''
p.write_text(s,'utf-8')
print('Updated plan, usage, authoring guide, import manifest and developer log.')
