from pathlib import Path
r=Path('.')
p=r/'使用说明.md';s=p.read_text('utf-8').replace('展示的牌逐张播放，正面完整停留1秒。','展示的牌逐张播放；v0.15 已将单张翻牌时间线加快到0.5秒。');s+='''

## v0.15 触发、费用选择与动画

触发条件满足后，即使没有合法目标也会显示堆叠。带“可以”的能力先入堆叠，再在右侧询问；不使用会撤回该次选发，且不消耗使用次数。必发缺少目标时不要求选牌，结算时无效果结束。有目标的响子仍须选满三张墓地牌，成功洗回后获得1点生命；不足三张不会凭空回复生命。

存在免颜色费用的使用方式时，先选择“不支付颜色使用”或“支付颜色使用”。后者继续正常的目标确认、自动／手动支付；提交前右键可取消，费用不会提前扣除。展示后授权使用的牌也采用相同选择；不支付颜色不免除角色条件、额外代价或适用税费。

硬币、D6和牌名／颜色宣言会在中央短暂显示，并写入对局记录。卡牌换区播放移动，包括牌库、手牌、战场、堆叠、颜色盘、墓地、除外、自机区及调试移动；隐藏区域之间不泄露对手的牌。表现完成后恢复原流程。

启动能力在拥有执行权且满足费用、目标与卡面限制时，可以在对手回合、战斗响应等窗口使用。按本轮确认，疾行同时解除新进场单位的宣攻与横置启动限制，并立即消除召唤失调雾环；失去疾行后仍按进场时刻判断。时符没有任意增减计时的操作；只有明确的卡牌效果（例如十六夜咲夜的自机能力）才提供相应选择。
''';p.write_text(s,encoding='utf-8')
p=r/'docs/触发能力时机与排序.md';s=p.read_text('utf-8').replace('## v0.9.4 交互与状态','## 当前交互与状态（v0.15）').replace('4. 选发触发在右下角显示“是否发动xxx的触发效果”。有目标时选目标并确定才入堆叠；无目标时直接确定。点“不使用”跳过，不增加使用次数。','4. 选发触发也先显示堆叠，再在右下角询问“是否发动xxx的触发效果”。有目标时选目标并确定，无合法目标时仍提供确认或不使用。确认补全已有堆叠对象；点“不使用”移除该次选发，不增加使用次数。').replace('没有合法目标的触发继续按现有引擎跳过，避免产生无法完成的选择。','按2026-09-19用户要求，没有合法目标的触发仍入堆叠：必发不弹目标选择，结算时无效果结束；选发仍询问，接受也不会虚构目标或执行依赖目标成功的后续收益。');p.write_text(s,encoding='utf-8')
p=r/'docs/召唤失调规则与修改入口.md';s=p.read_text('utf-8').replace('整理日期：2026-09-18，项目版本 v0.9.3。本轮仅调取与整理，未修改召唤失调判定。','原文整理于2026-09-18；当前实现更新至2026-09-19 v0.15。用户明确确认“疾行同时解除横置启动限制”，该项目裁定优先于下述原始词条仅明确提及攻击的范围。').replace('不能；当前疾行也不免除此限制 | 单位节2.21；疾行原文仅提攻击','通常不能；具有疾行时允许 | 2026-09-19用户确认的项目裁定').replace('但当前不会解锁其需要横置的启动能力','也立即解锁其需要横置的启动能力').replace('疾行仅在此放宽召唤失调','疾行已统一由summoning_sick返回不受限').replace('疾行例外只放宽攻击，故疾行单位仍可能有雾环。','v0.15 中原生、临时授予和光环授予的疾行都使雾环立即消失。').replace('2. 若疾行也要允许横置启动能力，需要同步修改 `activation_error` 和相关扩展能力检查，并补充该规则裁定。','2. v0.15 已通过统一 `summoning_sick` 使疾行同时允许横置启动；所有通用、扩展、预组及批次能力都复用此判定。').replace('4. 目前模型主要记录进场时刻，尚无通用“控制权变更”系统；将来加入夺取控制权时，需要额外记录开始操控的时刻，不能只看进场时间。','4. 临时控制、归还控制等效果已记录新的操控者回合计数；换区重新进场仍记录新时刻。').replace('本轮交互调整不改变这些规则。','v0.15额外验证原生／获得疾行和对手回合横置启动。');p.write_text(s,encoding='utf-8')
p=r/'docs/游戏整体开发计划.md';s=p.read_text('utf-8');s+='''

## 38. v0.15：统一触发入堆叠与表现事件

触发状态按“事件收集→同操控者排序→发布堆叠对象→选发／目标确认→响应→结算”运行。零合法目标用明确的 no_legal_targets 标记保留事件，必发不生成无法完成的选择，结算安全跳过效果；选发仍发布并询问。续程选择不是新的触发，继续由原结算负责。魔理沙的进场能力改由通用进场入口收集，犬走椛宣告与永久性进场诅咒通过堆叠结算，小夜岚咲夜观察双方单位进场。

免费使用偏好为私有声明状态，统一接入普通手牌／区域使用和效果授权使用。颜色费用层可显式采用通常费用，权限与费用分开；付款、公开声明前可以取消。未来新增免颜色效果须接入相同入口，不能直接把支付字典清空后绕过询问。

表现队列新增 move 与 result 两类快照，与 reveal 同序播放。shift、token消失、自机返回和控制权变化记录来源与去向；场上和手牌节点在表现完成后同步最终状态，避免再播放一次移动。战斗碰撞与伤害停留优先完成。隐藏区快照由视图按公开性显示卡背；队列不能作为查看隐藏牌的权限。单张翻牌时间线0.5秒、换区移动0.3秒、结果文字停留1.25秒。

疾行按本轮确认同时解除宣攻和横置费用的召唤失调限制；动态授予／失去均由统一判定控制。计时调整必须有正数放置事件和有效的明确替代能力来源，不产生时符自身的任意操作。

回归入口 tests/test_v015_rules.gd、tests/test_v015_ui.gd；完整进场与启动入口检查清单位于 work/v015/etb-audit.json、activation-audit.json。后续修改需保留无目标触发、反复目标选择、取消费用、隐藏信息、双方执行权、动画后恢复与原卡组不变这些约束。
''';p.write_text(s,encoding='utf-8')
print('usage and rule documents updated')
