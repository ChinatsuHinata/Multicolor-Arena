# multicolor:arena · 1.1

当前分享包：`builds/MulticolorArena-1.1-win64.zip`。包含此前悔棋、响应双按钮、横向预览、25张新卡，以及本轮截图中的修复。详见[1.1更新与测试记录](docs/1.1更新与测试记录.md)。

双方及观众使用同一版完整 EXE/PCK。个人 `.mdeck` 可以继续导入；旧版未结束对局及回放受规则版本校验。ZIP 旁的 `CheckTransfer-1.1.cmd` 校验传输完整性，解压后的 `VerifyFiles.cmd` 校验游戏文件。

本次更新：独立 `.mdeck` 卡组、30秒掉线整场判负、观战与 `.mreply` 回放。操作见[卡组分享、观战与回放](docs/卡组分享、观战与回放.md)。卡组位于 EXE 同目录 `deck`，回放位于 `replay`。

在 Godot 中运行 `main.tscn`。窗口为 1600×900，并默认最大化。

## 开始测试对局

1. 主菜单进入「人机对战」，点击「载入测试卡组」。
2. 双方选中「预组-灵梦」「预组-魔理沙」，各50张主卡加1张自机，使用正常构筑检查。
3. 点击「开始对战」，完成先后手选择。起手 4 张，点选想换的牌后「调度」，或直接「保留」。
4. 正常对局会在生命归零、抓空牌库或投降时结束，之后可返回准备页。

两套预组已正式保存，原有4副卡组保留。载入按钮恢复内存中的两套预组模板，不立即修改存档；编辑后点击保存才写入。套牌数量、卡牌清单和新效果说明见 `docs/新增卡片与预组-v0.10.md`。

组卡器开放全部已登记的可构筑卡，同一张牌的兼容异版不重复列出；衍生物和禁止常规构筑的梦违单位供相应效果在对局中生成或取得。可按类别、完整名称或颜色检索卡牌；颜色按钮严格匹配整组颜色，搜索栏可查找包含指定颜色的牌。主卡组严格50张，自机独立于这50张；保留自机同名牌不能加入主副卡组的限制。

## 对战操作

- **凭依**：可选的竖置调色盘牌显示蓝边。点击一张变为黄边，再次点击取消；随后点击一张手牌（黄边），最后点击「确定凭依」才交换。手牌也可再次点击取消选择，或直接「跳过凭依」。
- **出牌**：点击可用手牌、自机或可发动的区域卡片，卡牌与推荐费用立即显示金边。合法目标显示蓝边，点选后变金边；完成目标和费用选择后，右下角点「发动」一次提交。右键其他区域取消整个使用声明。
- **推荐付款**：系统先计算八云蓝等减费，再推荐费用。优先使用道具、单色资源，尽量保留双色资源和一次性红薯；推荐期间不横置任何牌。
- **调整付款**：点击金边资源取消该来源，其余预留保留；能补足余费的资源显示蓝边。点击蓝边资源后自动选择合适颜色，无需单独选择颜色。费用未选满时不显示「发动」；选满后仍需点击「发动」才扣费。右键取消不公开动作。
- **红薯**：仅后手初始持有，在颜色盘旁显示；不计入颜色盘数量。支付任意一种颜色的 1 点，实际支付后消失。
- **攻击与异能**：单位只有「攻击」一种可用行动时，点击先以金边和剑标记预选，右下角出现「攻击」。点「攻击」才宣告，右键其他位置取消；同时可以攻击和发动异能时，在居中的窗口展示行动卡面供选择。右键其他区域关闭选择。召唤失调用卡面外围缓慢流动的淡蓝色雾环表示，不添加文字；到操控者下一回合重置后消失。疾行同时解除当回合宣攻和横置启动的召唤失调限制，并移除雾环。
- **阻挡与伤害**：攻击响应结束后，没有合法阻挡单位会自动继续，不显示阻挡询问；有合法单位时点选再确认，也可不阻挡。进攻方分配多个阻挡者的伤害时，显示前 n−1 个阻挡者的无边框卡面，用下方「− / +」分配；最后一个自动承受剩余伤害。单位之间按攻击力结算，未被阻挡时按灵力伤害玩家。
- **观察战场**：支付、目标、异能、调度、凭依、阻挡、伤害分配等选择期间，右上角可点击「观察战场」暂时收起选择界面。可右键看牌、查看墓地；镜头固定在最近档；再点「返回选择」恢复原界面，已选费用、目标和伤害数字均保留，观察期间不会推进人机或提交行动。
- **高速与反制**：拥有执行权时点击高速符卡，选择蓝边目标，确认推荐或调整费用后点「发动」。进入堆叠后箭头指向实际目标；双方让过只结算最上方对象，之后重新开放响应。
- **目标与触发**：场上合法目标蓝边、点选金边。墓地、牌库、除外区的目标以区域卡面窗口选择，点卡面后点窗口「确定」，复合目标继续选下一项，最终确认发动。窗口中的卡牌可右键预览，已选内容保留。模式、颜色仍在右侧选择。必发能力先入堆叠再选目标；选发在右下角询问，可不使用。同时触发按居中窗口中的卡面选择顺序，堆叠卡面下显示本次发动的异能。
- **操作区域**：左侧为缩小后的卡牌预览，中央为战场和手牌，右侧为区域信息栏与行动按钮。设置、行动选择、触发排序、区域选牌、伤害分配和结果窗口统一居中；常驻攻击、响应、调试与确认按钮仍在右侧。
- **生命条**：双方生命区域带血条，按0—20点显示比例；高于20时血条保持满格，数字仍显示实际生命值。右侧文字显示玩家的铜钱、防避等状态。
- **响应模式**：右上角「全响应」「不响应」只能选中一个；再点已选按钮取消，两者都不选为默认。全响应在每个合法响应窗口询问；默认只在有合法响应时询问；不响应自动让过。主要阶段出牌、阻挡、凭依、调度不受影响。切换为不响应会取消尚未提交的响应预选。
- **联机悔棋**：对抗为空时点右上角「悔棋」，对方同意后回到上一项已完成操作之前，整条对抗一起撤销；不跨上一局。等待时可查看战场。回放不保留请求或撤销分支。
- **回合推进**：点「结束主要阶段」；进入结束阶段等阶段时，屏幕中央以无边框文字短暂显示名称。英勇、计时、弃牌和其他必要选择处理完成后换人。
- **设置与投降**：右上角「设置」中可继续游戏或投降；投降再次确认后结束。
- **查看卡片**：横向卡图在预览与堆叠中横置，其他位置保持竖置。右键手牌、战场、自机、颜色盘、墓地顶牌或堆叠，左侧显示大图、完整卡名和可滚动原牌文；关键词不附加解释，无能力牌留空。实际实例拥有的指示物显示在原文之后，属性指示物合并为“+X/+X/+X”或“-X/-X”，其他指示物按名称和数量显示。普通对局只显示对手手牌数量；牌库仍显示牌背和数量，隐藏信息不会泄露。
- **查看牌堆**：开局预览和牌堆窗口均关闭。点击战场上双方的牌库、墓地或除外牌堆，右侧出现与左侧预览等大的2列卡图，可滚轮查看全部卡片；不重复显示左侧已有的说明。按 G 打开或关闭己方墓地，按 H 打开或关闭己方除外区，提示位于画面左下角。点击另一牌堆切换，右键窗口外关闭。普通模式双方牌库通常只显示卡背；有明确允许查看自己牌库顶的效果时只公开该顶牌。调试模式可见卡面。浏览本身不会阻止回合继续。除外区位于各自自机区下方，仅有牌时出现。
- **区域手牌标签**：墓地、除外区、颜色盘或牌库中存在当前可以使用的牌时，手牌区上方才出现对应标签。切换后显示该区域的可用卡牌，点击后沿用选目标、确认、支付流程；没有可用卡时自动回到手牌。调试模式接管的一方同样可用。
- **场面标识**：可以交互的卡牌蓝边、选中黄边，进入堆叠不继承选中边。颜色盘与战场永久物左上角显示纯色六边形，每个占卡宽的1/5，五色正好排满。攻击力 / 剩余血量 · 灵力高于原值为绿色、低于原值为红色；横置永久物与颜色盘变暗。
- **战斗动画**：攻击者显示剑、阻挡者显示盾。伤判时双方单位碰撞，多阻挡逐一展示；没有阻挡则攻击单位向正前方冲撞。双方伤害计算后，场面与数值保持1秒，剩余血量可显示负数，再统一显示死亡单位离场。先制与普通伤害分别执行；先制伤判死亡者不再参与后续伤判，也不会重复造成先制伤害。抽牌、入堆叠、进场、横置等移动动画保留，战场不随阶段重建。
- **妖梦与先制**：自机妖梦或其半灵先造成伤害。被打死的单位先送墓并触发死亡效果，妖梦不能再用此次异能移除墓地中的它；存活单位在除外异能结算后离场，不参加后续伤判。如果除外被反制或没有有效的自机能力，仍在场的非先制单位继续造成普通战斗伤害。除外能力仍正常进入堆叠并允许响应。

自机单位需要战场永久物提供其全部颜色；颜色盘本身不满足该约束。角色符卡要求战场上有对应角色；具有「支援」时，自机区的对应角色也满足条件。灵梦与魔理沙各自的同称号单位不能同时存在。

## 组卡器

左侧预览、中央主副卡与自机、右侧卡库的布局不变。

- 顶部「组卡教程」可随时查看选牌、编辑、保存和搜索方法。双击中央上方的卡组名称可重命名，也可继续使用右侧「重命名」按钮。
- 顶部「卡组截图」会将当前草稿的自机、全部主卡和副卡导出为 PNG，保存到游戏旁的 `deck/image`，完成后显示完整路径；「打开 deck 文件夹」可直接查看卡组文件和截图。
- 从右侧卡库拖入主卡组、副卡组或自机位来加入；拖回卡库移除；主副卡组之间可移动。
- 单击卡名加入、中央左键复制、右键删除继续可用。
- 支持完整称号搜索、多色筛选、新建、重命名、保存和卡组代码分享。「导出代码」会复制精简代码到剪贴板并显示内容；「导入代码」可粘贴新代码或旧版 JSON，导入为新卡组。顶部「排序卡组」分别整理主卡组和副卡组，按类别、总费用、完整卡名排序，相同牌相邻；张数和自机不变，点击保存后写入。
- 颜色筛选严格匹配整组颜色：选黑色只显示纯黑卡，选红色和黑色只显示红黑双色卡；不包含单红、单黑或其他多色卡。点「全部」或取消所有颜色恢复全部卡牌。
- 搜索栏可直接输入颜色和类别，例如「红蓝黑单位」「红黑结界」「黄道具」「红蓝自机单位」；颜色表示卡牌包含这些颜色。「单位」包含普通单位与自机单位，「普通单位」和「自机单位」可分别搜索，类别下拉框采用相同区分。前面加「单」只找单色牌，例如「单黄符卡」。单独输入颜色或类别也可搜索；颜色按钮仍按上面的严格规则筛选。
- 搜索栏也可按种族筛选，例如「人类」「妖精」「单蓝妖精」「红黑人类」「红黄吸血鬼」。结果同时包含与该种族相关的单位、符卡、道具和结界；颜色表示包含所列颜色，「单」限定只含一种颜色。梦违卡和衍生物卡不在卡库中显示。
- 输入自机名称或名称片段加「符卡」可找到该自机角色的全部角色符卡，例如「比那名居天子符卡」或「天子符卡」。若名称片段同时是颜色名，结果会合并颜色匹配的符卡与该自机的角色符卡。
- 可在 `data/card_search_aliases.json` 增删搜索别名。普通别名以卡名片段数组填写，例如 `"西瓜": ["伊吹萃香"]`；「西瓜符卡」也会找到萃香的角色符卡。新增「老师」「小五」「恋恋」分别对应上白泽慧音、古明地觉、古明地恋。特殊规则「炮」匹配卡名或既有别名为恋符「极限火花」的牌，「饼」匹配带横置产色能力的2费单色道具。大康对应「梦想封印-瞬-」，小康对应「梦想封印-散-」；蓝康对应「青ノ花」，绿康对应「妖精ノ巡礼」。
- 搜索「密封」显示命定的重逢自机、大空魔术及两张秘封梦违单位；「密封符卡」精确显示卯酉东海道、莲台野夜行、梦违科学世纪及其三张梦违符卡，共六张。「530」可找到「5:30 PM」。输入「秦心符卡」也会显示「假面丧心舞 暗黑能乐」；该牌已无支援属性。
- 点击空的自机框或已放入的自机卡图，会打开自机选择窗；点击其中的卡图即可设为自机。选择窗可按名称筛选，当前自机以金色边框标出。拖放仍可设置自机，右键已放入的自机可移除。
- 右下角「删除卡组」删除当前卡组，确认后同步保存列表；未保存的新卡组只清除草稿。删除最后一副后进入空白卡组。原「备牌」按钮移除，副卡组仍可通过拖放编辑。
- 主卡布局10列×5行，一屏可见50张主卡及1张自机，超过50张继续向下排列并滚动查看，副卡区独立保留。
- 保存要求有自机，容量为主卡 70、副卡 10、自机 1。草稿不用恰好 50 张。
- 正常对战检查主卡 50 张、主卡同名最多 4 张（终言同名最多1张，限制级同名最多2张）、主副卡不含所选自机同名牌；测试开关跳过这些构筑检查，未知卡、无自机、坏数据和容量上限仍拦截。

## 数据与维护

- `cards/*.json`：511条卡的中文资料、费用、构筑资格、能力绑定，组卡器和对战共用。
- `recourse/数据库/`：已经入库的卡图，按稳定卡牌ID命名；`卡图索引.json`列出名称、数据路径、卡图路径与校验值。
- `recourse/新增卡片/`：待处理区。放入图片不会自动导入游戏；之后按指定批次先移动卡图到数据库，再编写和登记卡牌。v0.13本批395张图片已处理，后续新放入的图片仍须指定批次后导入。
- `tools/check_card_database.py`：只检查现有已登记卡图与清单；`--write-index`更新清单，不新增卡牌。
- `scripts/card_database.gd`：卡池登记、字段和能力校验。
- `scripts/rules/duel_engine.gd`：回合、支付、区域、堆叠、战斗、人机。
- `scripts/rules/demo_abilities.gd`、`expanded_abilities.gd`、`precon_abilities.gd`、`excel_abilities.gd`、`mask_abilities.gd` 和 `catalogue_*.gd`：各批卡池的能力实现。
- `scripts/duel_view.gd`：交互、私有预付、查看面板、行动卡面、阶段提示和设置。
- `scripts/duel_reveal.gd`：公开展示队列、逐张翻牌、一秒正面停留和临时手牌公开。
- `scripts/duel_hand_card.gd`：双方手牌展示、右键查看和点击出牌入口。
- `scripts/duel_table.gd`：3D 战场与鼠标选牌；每张牌堆高度 0.012；桌垫区域中心由 MAT_SLOTS 配置。
- `scripts/main.gd`：菜单和组卡器；顶部 `battlefield_background` 可修改桌垫图片。
- `docs/卡牌数据库与编写说明.md`：如何改数值、添加能力和登记卡牌。
- `docs/游戏整体开发计划.md`：状态说明、实现范围和后续工作。
- `开发者日志.md`：每轮开发记录。

当前登记511条卡牌定义，支持本地人机、调试及局域网对战；包括梦违取得、复制、替代使用与本轮双自机。尚未覆盖Excel全牌表。最新源码暂未导出。


## 关于与调试

在人机对战设置页勾选「测试模式（手动控制双方）」即可开启，无需密码；旁边的「无需付费」可跳过颜色费用，对局内顶部也可切换。对局顶部的「测试说明」可随时查看区域点击、快捷键和调试拖放教程。模式只存在本次运行，重启默认关闭。原有的「关于」页输入 `multicolor` 方式仍可使用。

调试对局公开双方手牌。顶部「操作：你／人机」指示当前由你操作的一方；调度、凭依、出牌、付款、攻击、阻挡和响应按当前执行权或选择所属方切换，人机不自动代打。人机手牌也从上方手牌区点击使用。三档响应仍按原定义工作。

点击战场上己方或对手的牌库，可打开整副剩余牌库，保留正在进行的付款和选择。右键查看卡图和文字，角落可观察战场，再返回选择。

「调试」打开右侧区域列表，再点同一按钮收起并返回对局，也可以点击列表右上角「×」、按 Esc 或右键列表外关闭。按钮高亮表示列表已打开。「观察战场」时仍可点击「调试」返回。通过按钮、×或Esc返回会保留原有付款、目标或行动选择；右键列表外仍会取消尚未提交的私有出牌声明。以上操作均不跳过必发选择，也不关闭双方手牌公开与人工接管功能。

区域列表可切换双方及来源区域。也可直接点场上的牌堆，滚动找到卡牌，按住卡面拖到所属玩家的战场、颜色盘、手牌、牌库、墓地、除外区或合法自机区。也可以直接从手牌和场上卡面开始拖动。移入牌库时放在最上方，下一次抽牌即抽该牌；卡图上方提示松手后的目的区域。拖动时列表暂时收起，松开后恢复；右键或拖到无效位置取消。放入战场后自动排列。

对抗和选择均为空时，左键点击双方战场上的空白单位、道具或结界区域，可搜索并加入对应类别的任意卡牌，包括梦违与衍生物。按 Z、X、C 可分别向当前回合玩家的手牌、颜色盘、墓地加入任意卡牌；X 加入颜色盘的牌为竖直状态。新卡直接进入目标区域，不触发进场或离场效果。

调试拖放直接移动现有实例，不付款、不进入堆叠，不触发进场、离场、死亡或计时替代效果。拥有者保持；自机区仅接收本局所选自机，普通符卡不能驻场，单位仍受容量和同称号限制，衍生物离场消失。必须先完成或取消正在进行的选择和支付。正常点击手牌使用仍遵循原规则，并正常触发卡牌效果。调试只改变当前对局，不改保存的卡组。

## 新增卡效果的操作

模式、颜色与费用在右侧逐步选择；墓地、牌库及除外区目标打开卡面窗口；战场目标直接点蓝边对象，确认前可取消或重选。异星弹幕先选第一项再选第二项，可重复。桑尼进场的必发能力先进入堆叠，随后可选自身、己方或对方合法单位，点击「确定」补全目标，结算后目标本回合获得疾行。可选触发可以「不使用」。墓地卡可在墓地列表发动；满足条件时，Hell's Tokamak 可从手牌区的「牌库」标签使用。结晶、贫乏、奇迹、计时与先制已接入本批卡的规则。

新卡清单与绑定见 `docs/新增卡片导入清单-v0.9.md`。卡图统一显示尺寸，原文件保持不变。


华胥的亡灵「西行寺幽幽子」自机触发每次造成1点伤害，每个回合至多发动两次。每次死亡分别询问、选目标、确认；第二次不会沿用第一次的目标。同一次死亡对同一个能力来源只生成一次触发，选择不使用不消耗发动次数。JSON绑定的「伤害」「每回合次数」供后续调整。

召唤失调相关原文、疾行和阻挡例外、当前代码入口见 [规则摘录](C:/Users/tzx20/Documents/test/docs/召唤失调规则与修改入口.md)。本轮不修改这些规则。

## v0.9.5 对局记录与新增卡

右上角「对局记录」显示整场对局的回合、阶段、出牌、结算、攻击、卡牌换区、伤害与生命变化。记录在最上层，打开时暂停自动推进，其他操作不能穿透；必须右键记录面板外关闭。原有选择与支付预留会保留。记录里对手的未公开抽牌仍为卡背，不能泄露牌名。记录只保留在当前对局内，尚未提供导出或重放。

普通副本的自机能力在左侧文字预览中为灰色；本局所选自机、场上受博丽神社影响的自机单位、带自机指示物的对象按实际规则显示正常颜色。普通能力与关键词不变灰。

新增地狱的轮祸「火焰猫燐」、「未应ノ爱」、蓬莱的人之形「藤原妹红」、未确认幻想飞行物「封兽ぬえ」。具体费用与能力见 `docs/新增卡片导入清单-v0.9.5.md`。封兽ぬえ费用已按用户确认更新：红／蓝／绿任意组合共3点，另加1黑，总计4点；牌色为红蓝绿黑，在颜色盘可任选其中一种产费。三点可以同色，也可以混色。


## v0.11 新卡与Excel规则

新增卡片目录的两批170张图已入库，卡池共251条定义（242可构筑、9衍生物）。重新运行项目即可在组卡界面使用新卡；「预组-灵梦」「预组-魔理沙」及其他已保存卡组保持不变。

预览文字、费用和效果按Excel；原卡图保留，印刷内容有差异时以左侧文字及实际规则为准。额外弃牌使用卡面窗口，确认前右键可取消；多种启动异能分别选卡面；神子的X点伤害按右侧提示逐点分配，同一单位可分配多点。河童背包触发时选择支付一种颜色1点或不支付。

详细入库清单、来源差异和测试结果见 `docs/新增卡片与Excel核对-v0.11.md`。


## v0.12 新卡与半灵

新增目录87张卡图已处理，新增15张可构筑牌与3张面具衍生物，卡库共269条。资料及实际效果按Excel，图上旧文字不覆盖Excel。半灵已换为这次提供的卡图；重新运行游戏即可加载。

秦心触发后选心情，面具点击发动并牺牲；勇者琪露诺的勇气数量显示在场上和预览中。喜怒哀乐附体按道具数量逐项选择效果与目标，完成后统一确认；反制税费由被询问玩家选择支付。两套预组和其他已存卡组未改变。

## v0.13 新卡与使用方式

本批395张原图已归档，新增217种卡及全部对应效果。卡池共486种；资料以Excel为准，村纱水蜜的“+X”按用户确认增加灵力。重新运行项目即可加载；原有六副卡组和两套预组模板没有改动，半灵继续使用上轮替换的图片。

宣告牌名时在右侧搜索并选择完整名称后确认。效果允许使用墓地、除外、颜色盘或牌库顶的牌时，使用相应区域标签；局外梦违通过效果选牌，不能直接加入卡组。额外弃牌、牺牲等仍先选卡，确认付费才提交；免费使用不自动免除角色条件。

触发效果要求付款、或展示顶牌后允许付费使用时，同样显示推荐费用，可点击金边取消、蓝边替换。取消支付回到待完成的选择，费用不会提前扣除。已有浮动颜色值可在付款界面选择使用。右键检查与观察战场保持可用。

完整卡单、来源和验证结果见 `docs/新增卡片与Excel核对-v0.13.md`。

## v0.14 展示与战场布局

展示的牌逐张播放；v0.15 已将单张翻牌时间线加快到0.5秒。展示手牌时仅对应牌临时公开，随后恢复原来的隐藏状态；牌库顶/底每次均翻牌，重复展示同一张也重新播放。牌库底从最底下的一张开始展示。展示期间暂停自动推进及提交新行动，播放完后继续原有目标选择、支付或结算。

战场与己方手牌区水平居中，镜头固定最近档，滚轮只滚动牌堆等窗口。左右查看窗口均为218×660，右侧每行2张卡，调试模式也可完整看到4行。普通对局顶部只显示对手手牌计数；调试模式仍可看到并操作对手手牌。

双方单位在中央前排；道具在各自后排左半边，结界与时符在后排右半边横放；颜色盘在最后一排。这四类使用同一张卡尺寸。乐章横放于中央两侧，我方使用右侧，对方使用左侧。场上只留一个乐章，所以两侧不会各放一个生效乐章。

按2.34f，新乐章生效进场时，无论旧乐章由谁操控，均将其作为规则动作送到原拥有者墓地。这个替换不入堆叠，不属于消灭；新乐章被反制时不替换旧乐章。调试直接摆放也保持唯一性，同时继续不触发进离场效果。

左侧预览新增完整指示物列表，包括属性、自机、计时、贫穷、惊吓、勇气、梦违、狂乱和各色指示物。改变数量、移除指示物或换区时自动刷新。


## v0.15 触发、费用选择与动画

触发条件满足后，即使没有合法目标也会显示堆叠。带“可以”的能力先入堆叠，再在右侧询问；不使用会撤回该次选发，且不消耗使用次数。必发缺少目标时不要求选牌，结算时无效果结束。有目标的响子仍须选满三张墓地牌，成功洗回后获得1点生命；不足三张不会凭空回复生命。

存在免颜色费用的使用方式时，先选择“不支付颜色使用”或“支付颜色使用”。后者继续正常的目标选择和推荐付款调整；提交前右键可取消，费用不会提前扣除。展示后授权使用的牌也采用相同选择；不支付颜色不免除角色条件、额外代价或适用税费。

硬币、D6和牌名／颜色宣言会在中央短暂显示，并写入对局记录。卡牌换区播放移动，包括牌库、手牌、战场、堆叠、颜色盘、墓地、除外、自机区及调试移动；隐藏区域之间不泄露对手的牌。表现完成后恢复原流程。

启动能力在拥有执行权且满足费用、目标与卡面限制时，可以在对手回合、战斗响应等窗口使用。按本轮确认，疾行同时解除新进场单位的宣攻与横置启动限制，并立即消除召唤失调雾环；失去疾行后仍按进场时刻判断。时符没有任意增减计时的操作；只有明确的卡牌效果（例如十六夜咲夜的自机能力）才提供相应选择。


## Windows 分享版（1.0）

可直接分享 `builds/MulticolorArena-1.0-win64.zip`。对方完整解压，双击 MulticolorArena.exe；同名 PCK 保持在旁边，无须安装 Godot。首次运行带入本次导出时保存的 4 副卡组，个人修改保存在 user://，重启不会覆盖。

今后每次导出会由“卡组发行打包”插件自动携带当时保存的全部卡组。具体步骤和过滤配置见 [Windows导出与分享](docs/Windows导出与分享.md)。


## v0.16 堆叠查看

堆叠显示在战场右侧的独立区域，最新对象在上方。卡图放大并正立显示，异能文字在图下；对象较多时滚动查看。右键卡面可在左侧预览，使用反制时直接点击带蓝边的堆叠对象，再确认。卡面不会盖住场上的目标。

v0.18.0 分享包已经包含上述堆叠调整及局域网 BO3。旧 v0.15.3 包保留为历史版本。


## v0.17 付款调整

点击可使用的卡牌或选择异能后，系统立即推荐费用：当前卡牌和推荐资源显示金边。点击某个金边资源可取消它；能补足剩余费用的资源变成蓝边，再点击所需资源即可，系统自动分配适合的颜色。八云蓝等减费先计入推荐。费用未选满时不显示发动按钮。

完成目标与资源选择后，点右下角“发动”才真正扣费；右键其他区域取消整次使用。推荐期间资源不会横置，红薯也不会消耗。手动支付入口和自动支付开关已移除。免费使用选择仍保留，免费也需点击发动。

## v0.18.1 局域网对战

两台电脑完整解压同一发行包，连接可互通的同一局域网。进入“联网对战”修改显示 ID；一方创建房间，另一方点击房间列表加入，房主接受申请。未发现时可填写房主页面显示的 IP 地址和端口加入。

双方选择卡组并点击“准备”，首局双方投 D6，平点重投，点数高者选择先手或后手；BO3 后续局由上一局败者选择，平局沿用上一局选择者。选择结果向双方展示并进入记录，重连不重投、不重选。支持 BO1 / BO3；“本局投降”只结束一局，BO3未分出胜者时点击“下一局”继续。局间可调整主副卡组，保持登记总牌池、自机和主卡组张数，两胜结束整场。

联网付款沿用 v0.17 的金边推荐与蓝边替换，点“发动”才提交，取消不公开。各自只看到自己的手牌，联机禁止调试移牌。断线检测后锁住所有对局操作，仍可查看战场。30秒内同步重连成功则继续；超时终止整场，掉线方判负且不可恢复该对局。需要双方保留本机恢复记录。

本机双进程完整对局与恢复检查已通过，真实双机验证待完成。详细操作与测试项目见[局域网双机测试](docs/局域网双机测试.md)。公网平台中继尚未接入。

房间和战场右上角显示双方网络延迟（ms），约每2秒更新，表示到对端的往返时间。断线显示“已断开”，重连后重新测量；延迟刷新不会取消选牌或费用草稿。

异地玩家可以借助通用虚拟局域网，用房主虚拟IP加入，具体见[虚拟局域网异地联机](docs/虚拟局域网异地联机.md)。双方需使用相同版本，本版不恢复旧版未结束的联机对局，个人卡组仍保留。


## v0.18.2 规则修复与文件校验

自机幽幽子因伤害、消灭或牺牲而死亡，选择返回自机区后仍可发动打1；同回合最多两次。一般回手、除外或其他非死亡移动不触发此效果。结晶换入颜色盘的卡竖直、未横置进入，可供后续付费。

复制游戏包后，先将压缩包旁的 `CheckTransfer-1.0.cmd` 与 ZIP 放在同一目录并运行，显示 PASS 后完整解压；再运行包内 `VerifyFiles.cmd`，EXE/PCK 均应 PASS。详细排查见《Windows导出与分享》。联机双方须同时升级；卡组保留，旧版未结束联机对局不恢复。


## 1.0 图形换备牌与正式版本

BO3局间点击“调整主副卡组”，点击卡片或拖动在主牌与备牌间移动，右键查看。右侧全卡池隐藏，自机锁定。点“更换完成”时检查卡组；通过房主确认后回到准备页。原来保存的卡组不改写。已准备可先取消准备，再修改卡组。

门符「最后ノ理想国」费用为2蓝1绿；「冬」费用为2蓝1黑。

正式名称multicolor:arena，版本1.0。双方联机须更新同版，升级后仍使用原来的个人存档目录。
